# Launch copy (X thread · Show HN · Reddit)

Reusable copy. Edit the *current reality* line to match the README before posting. No
hype beyond what we can show.

## X / Twitter thread

1/ Intelligence got ~98% cheaper. Open models like GLM 5.2 now beat the frontier on the
broad middle of everyday work. So why is nobody switching?

2/ Because a model is a brain in a jar. What you actually run is the *harness*: context,
routing, tool-calls, prompts, surfaces. Switching models means rebuilding all of it.

3/ The talent to build a harness is scarce and expensive. So teams sign a frontier
contract instead — and the labs move their assistant *into your team chat* and quietly
absorb the context that is your edge.

4/ Then you're renting your own brain back. Forever. At frontier prices. With no way to
rip it out.

5/ glamfire is the open, self-owned harness that closes the last mile:
• own your context (local-first, portable, exportable)
• route each task to the cheapest capable model (GLM 5.2 on Fireworks by default)
• one harness across every model family — no rewrite to switch

6/ The harness-talent shortage isn't a wall. It's the opening of the decade. We're
building the harness in the open so anyone can own their last mile — not just the few
companies that can afford to hire for it.

7/ Apache-2.0. macOS/Windows/Linux. Honest "current reality" — we don't ship vaporware.
Come build it with us 👇
github.com/glamworks/glamfire

## Show HN

**Title:** Show HN: glamfire – own your AI harness, route to the cheapest capable model

**Body:** Open models (GLM 5.2) now match or beat the frontier on the center of
distribution — most everyday work — at ~1/5 the cost. But you can't just switch: the
*harness* (context, routing, per-model tool-calls, prompts) is the real lock-in, and
frontier labs are moving it into your team chat to absorb your context.

glamfire is an open, model-agnostic harness that (1) keeps your context local-first and
portable, (2) routes each task center↔edge to the cheapest capable model and escalates
on low confidence, and (3) runs one work system across model families via tested
adapters. Apache-2.0, cross-platform.

It's early — the README's *Current reality* section says exactly what works today vs.
what's specified. I'd love feedback on the router and the adapter contract. (No token is
required to use it; the software is independent of any of that.)

## Reddit (r/LocalLLaMA)

**Title:** Building an open harness to route GLM 5.2 (via Fireworks or self-hosted vLLM)
and keep your own context

Self-hosting GLM 5.2 is cheap and great for the center of distribution. The hard part
isn't the model — it's the harness: routing, owned memory, per-model tool-calls. I'm
building that in the open (Apache-2.0): center/edge cost routing, a local-first portable
context store, and tested adapters so you can swap GLM ↔ frontier with a config change.
Honest about what works today. Feedback from people actually self-hosting welcome.
