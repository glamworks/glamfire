{
  "name": "@glamfire/engine",
  "version": "0.0.1",
  "private": true,
  "description": "The open engine — the agent loop: plan to act to observe, tool dispatch, permissions, sandboxing, streaming (SPEC 5.1).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "typecheck": "tsc --build tsconfig.json --noEmit"
  }
}
